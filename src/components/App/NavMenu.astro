---
import InfoUser from "./InfoUser";
import MainMenu from "./MainMenu.astro";
import logo from "../../assets/logo_regio_rentas.png"

// pasar por componentes los datos que se enviaran o traer con un componente de react?
---

<aside class="bg-[#fafafa] flex flex-col h-dvh w-72 p-3 border-r border-slate-200">
    <div class="border-b border-slate-300 w-full p-2 mb-4">
        <img class="w-28 m-auto object-cover" src={logo.src} alt="Logo regio rentas">
    </div>
    <div class="flex-1">
        <InfoUser client:load />
        <MainMenu />
    </div>
    <form action="/api/auth/signout" class="">
        <button
            type="submit"
            class="px-4 w-full py-2 rounded-sm border border-slate-300 text-slate-600 font-semibold cursor-pointer hover:bg-neutral-800 hover:text-white duration-300"
            >Cerrar sesión</button
        >
    </form>
</aside>
